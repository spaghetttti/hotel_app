# PostgreSQL connection properties
spring.datasource.url=jdbc:postgresql://localhost:5432/hotel_app
spring.datasource.username=asilbekmuminov
spring.datasource.password=azizbekloh

# Create (if not exist) or update existing tables on startup
spring.jpa.hibernate.ddl-auto=create-drop

# Show SQL statements generated by Hibernate
spring.jpa.show-sql=true

# Enable Hibernate dialect for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


## Set the Hibernate naming strategy
#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl


